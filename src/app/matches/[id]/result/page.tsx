import Link from "next/link";
import { LeagueShell } from "@/components/league-shell";
import { getMatch, players } from "@/lib/data";

export default async function Result({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const match = getMatch(Number(id)); return <LeagueShell><div className="back"><Link href="/matches">← 返回赛事中心</Link></div><section className="result-hero"><p>FINAL RESULT · {match.bo}</p><h1>{match.name}</h1><div><b>{match.teams[0]}</b><strong>{match.score[0]} <i>:</i> {match.score[1]}</strong><b>{match.teams[1]}</b></div><span>{match.date} · 比赛已结束</span></section><section className="panel game-tabs"><button className="selected">GAME 1</button><button>GAME 2</button><button>GAME 3</button></section><section className="panel result-table"><h2>对局数据</h2><div className="result-head"><span>选手</span><span>K / D / A</span><span>KDA</span><span>输出</span><span>经济</span></div>{players.map((p, i) => <div className="result-row" key={p.id}><div><img src={p.avatar} alt=""/><b>{p.name}</b><small>{i < 5 ? match.teams[0] : match.teams[1]}</small></div><span>{8 + (i % 4)} / {2 + (i % 3)} / {6 + (i % 5)}</span><span>{p.kda}</span><span>{(12 + i * 1.7).toFixed(1)}k</span><span>{(9 + i * 0.6).toFixed(1)}k</span></div>)}</section></LeagueShell>; }
